import { Tree, NodeType, Parser } from "lezer";
declare function defaultIgnore(type: NodeType): boolean;
export declare function testTree(tree: Tree, expect: string, mayIgnore?: typeof defaultIgnore): void;
export declare function fileTests(file: string, fileName: string, mayIgnore?: typeof defaultIgnore): {
    name: string;
    run(parser: Parser): void;
}[];
export {};
